<!-- /templates/customers/register.liquid -->

<section id="Register" class="center account">
  <section class="col wrapper wrapper--narrow">
    <div class="customer-register">
      <div class="account_title">CREATE YOUR ACCOUNT BELOW</div>
      {% form 'create_customer' %}
        <div class="form-field">{{ form.errors | default_errors }}</div>
        <div class="form-field">
          <div class="account_text">EMAIL</div>
          <input
            type="email"
            value="{{ form.email }}"
            name="customer[email]"
            id="email"
            class="large{% if form.errors contains 'email' %} error{% endif %}"
            placeholder="{{ 'customer.register.email' | t }}">
        </div>
        <div class="form-field">
          <div class="account_text">PASSWORD</div>
          <input
            type="password"
            value=""
            name="customer[password]"
            id="password"
            class="large password{% if form.errors contains 'password' %} error{% endif %}"
            placeholder="{{ 'customer.register.password' | t }}">
        </div>
        <div class="form-field">
          <div class="account_text">CONFIRM PASSWORD</div>
          <input
            type="password"
            value=""
            name="customer[password]"
            id="password"
            class="large password{% if form.errors contains 'password' %} error{% endif %}"
            placeholder="{{ 'customer.register.password' | t }}">
        </div>
        <div class="form-field">
          <div class="account_text">FIRST NAME</div>
          <input
            type="text"
            value="{{ form.first_name }}"
            name="customer[first_name]"
            id="first_name"
            class="large"
            size="30"
            placeholder="{{ 'customer.register.first_name' | t }}">
        </div>
        <div class="form-field">
          <div class="account_text">LAST NAME</div>
          <input
            type="text"
            value="{{ form.last_name }}"
            name="customer[last_name]"
            id="last_name"
            class="large{% if form.errors contains 'last_name' %} error{% endif %}"
            placeholder="{{ 'customer.register.last_name' | t }}">
        </div>
        <button type="submit" class="account_submit">{{ 'customer.register.submit' | t }}
        </button>
        <a href="{{ routes.account_register_url }}">
          <div class="createabtn">Sign in here</div>
        </a>
      {% endform %}
    </div>
  </section>
</section>